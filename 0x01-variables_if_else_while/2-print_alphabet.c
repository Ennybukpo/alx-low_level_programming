#include <stdio.h>

/**
 * main - Prints alphabets in lower case
 *
 * Return: Always 0.
 *
 */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
		putchar('\n');

	putchar('\n');

	return (0);
}
