#include "main.h"
/**
 * 
 * main - check the code
 *
 * Return: Always 0.
 *
 * print_alphabet: Prints the alphabet in lowercase, followed by a new line.
 */
void print_alphabet(void)
{
	char a;
for (a = 'a'; a <= 'z'; a++)
	_putchar(a);
_putchar('\n');
}
