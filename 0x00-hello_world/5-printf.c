#include <stdio.h>
/**
 * main-entry for the program
 * return:0 if no errors, return non zero if errors exist
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
