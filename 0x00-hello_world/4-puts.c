#include <stdio.h>
/**
 * main-entry point for program
 *
 * Return:0 if no errors, reurn non zeros i errors
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
